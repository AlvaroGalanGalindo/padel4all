{% extends '@App/base.html.twig' %}

{% block content %}
    <div class="content">
        <div class="row">
            <div class="page-header">
                <h2>Resultados</h2>
            </div>
            {% for pista in pistas %}
                <div class="col-xs-4 item-container">
                    <div class="item item-pista">
                        <div class="etiqueta">Nombre</div>
                        <div class="valor">{{ pista.nombre }}</div>
                        <div class="etiqueta">Propietario</div>
                        <div class="valor">{{ pista.propietario }}</div>
                        <div class="etiqueta">Localidad</div>
                        <div class="valor">{{ pista.localidad }}</div>
                        <div class="etiqueta">telefono</div>
                        <div class="valor">{{ pista.telefono }}</div>
                        <div class="botonera">
                            <a href="{{ path('pista_show', { 'id': pista.id }) }}" class="btn btn-info">Ver</a>
                            <a href="{{ path('pista_edit', { 'id': pista.id }) }}" class="btn btn-info">Editar</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
